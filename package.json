{
  "name": "test",
  "version": "1.0.0",
  "description": "This is a basic development environment with browser-sync and gulp-sass.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marco",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.8",
    "gulp-sass": "^3.1.0"
  }
}
